version: '3'

services:
  publications:
    image: docker.pkg.github.com/gh-network/publications/gh-publications:actions-test
    environment:
      - MONGO_ADDRESS=db:27017

  db:
    image: mvertes/alpine-mongo
